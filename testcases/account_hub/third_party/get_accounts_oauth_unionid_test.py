# NOTE: Generated By HttpRunner v4.3.5
# FROM: .\testcases\account_hub\third_party\get_accounts_oauth_unionid.yml
import pytest

from httprunner import HttpRunner, Config, Step, RunRequest
from httprunner import Parameters


class TestCaseGetAccountsOauthUnionid(HttpRunner):
    @pytest.mark.parametrize(
        "param",
        Parameters(
            {
                "name-unionid-status_code-msg": [
                    ["通过unionid获取用户信息 - 通过unionid查询", "$u_id", 200, "unionid"],
                    ["通过unionid获取用户信息 - unionid未注册查询", "dsadsaassa", 204, ""],
                ]
            }
        ),
    )
    def test_start(self, param):
        super().test_start(param)

    config = (
        Config("/accounts/oauth/{unionId} 通过unionid获取用户信息")
        .variables(
            **{"url": "${ENV(BASE_URL)}", "u_id": "${accounts_oauth_unionId($url)}"}
        )
        .base_url("${ENV(BASE_URL)}")
    )

    teststeps = [
        Step(
            RunRequest("$name")
            .get("/accounts/oauth/$unionid")
            .validate()
            .assert_equal("status_code", "$status_code")
            .assert_contains("text", "$msg")
        ),
    ]


if __name__ == "__main__":
    TestCaseGetAccountsOauthUnionid().test_start()
